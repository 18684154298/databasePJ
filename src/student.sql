CREATE TABLE student (
                      registno VARCHAR(255),
                      name VARCHAR(255),
                      kdno INT,
                      kcno INT,
                      ccno INT,
                      seat INT,
                      PRIMARY KEY (registno)
) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
